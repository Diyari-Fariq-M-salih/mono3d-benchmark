# Archived Scripts

These scripts were kept because they document useful one-off investigations, but they are not part of the normal benchmark flow anymore.

The active day-to-day entry points stay in `scripts/` at the top level.

Archived here:

- isolated or pose-conditioned EuRoC experiments
- external-pose reconstruction diagnostics
- report-layout maintenance helpers
- one-off split or validation utilities
